#include <stdio.h>
#include <stdlib.h>

void main()
{
	int Buff(5)=(5,15,30,10,35);
	int *p;
	p=Buff;

	int elemento;
	for (int i = 0; i < 5; ++i)
	{		
		elemento=Buff(i);
		printf("%d - ",  elemento);
	}

	for (int y=o ; i<5 ; i++)
	{
		
	}

}